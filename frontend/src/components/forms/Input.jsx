/**
 * Forms Input Component
 * Re-export the enhanced Input component from common directory
 * This maintains backward compatibility while using the improved version
 */

export { Input } from "@/components/common/Input";
